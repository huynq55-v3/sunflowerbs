# Workspace root — chỉ chứa crate WASM `rust-crypto` dùng cho frontend.
# `sunflowerbs` là tên gọi dự án; logic mật mã thuần nằm gọn trong `rust-crypto`.
[workspace]
resolver = "2"
members = ["rust-crypto"]

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
